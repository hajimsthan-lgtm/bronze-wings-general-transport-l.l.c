{
  "name": "Vehicle",
  "type": "object",
  "properties": {
    "plate_number": {
      "type": "string"
    },
    "image_url": {
      "type": "string"
    },
    "ownership_front_url": {
      "type": "string"
    },
    "ownership_back_url": {
      "type": "string"
    },
    "make": {
      "type": "string"
    },
    "model": {
      "type": "string"
    },
    "year": {
      "type": "number"
    },
    "type": {
      "type": "string",
      "enum": [
        "truck",
        "trailer",
        "tanker",
        "pickup",
        "crane",
        "other"
      ],
      "default": "truck"
    },
    "status": {
      "type": "string",
      "enum": [
        "active",
        "maintenance",
        "inactive"
      ],
      "default": "active"
    },
    "assigned_driver": {
      "type": "string"
    },
    "vendor_name": {
      "type": "string"
    },
    "registration_expiry": {
      "type": "string",
      "format": "date"
    },
    "insurance_expiry": {
      "type": "string",
      "format": "date"
    },
    "last_service_date": {
      "type": "string",
      "format": "date"
    },
    "next_service_date": {
      "type": "string",
      "format": "date"
    },
    "odometer_km": {
      "type": "number",
      "default": 0
    },
    "fuel_type": {
      "type": "string",
      "enum": [
        "diesel",
        "petrol"
      ],
      "default": "diesel"
    },
    "supply_start_date": {
      "type": "string",
      "format": "date"
    },
    "supply_rate": {
      "type": "number",
      "default": 0
    },
    "supply_rate_type": {
      "type": "string",
      "enum": [
        "daily",
        "monthly",
        "per_trip"
      ],
      "default": "daily"
    },
    "supply_status": {
      "type": "string",
      "enum": [
        "active",
        "returned"
      ],
      "default": "active"
    },
    "notes": {
      "type": "string"
    }
  },
  "required": [
    "plate_number",
    "make",
    "model"
  ],
  "rls": {
    "read": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "create": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}