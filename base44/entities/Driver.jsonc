{
  "name": "Driver",
  "type": "object",
  "properties": {
    "name": {
      "type": "string"
    },
    "image_url": {
      "type": "string"
    },
    "phone": {
      "type": "string"
    },
    "email": {
      "type": "string"
    },
    "license_number": {
      "type": "string"
    },
    "license_expiry": {
      "type": "string",
      "format": "date"
    },
    "nationality": {
      "type": "string"
    },
    "status": {
      "type": "string",
      "enum": [
        "active",
        "inactive",
        "on_leave"
      ],
      "default": "active"
    },
    "assigned_vehicle": {
      "type": "string"
    },
    "vendor_name": {
      "type": "string"
    },
    "base_salary": {
      "type": "number",
      "default": 0
    },
    "join_date": {
      "type": "string",
      "format": "date"
    },
    "emergency_contact": {
      "type": "string"
    },
    "visa_expiry": {
      "type": "string",
      "format": "date"
    },
    "supply_start_date": {
      "type": "string",
      "format": "date"
    },
    "supply_rate": {
      "type": "number",
      "default": 0
    },
    "supply_status": {
      "type": "string",
      "enum": [
        "active",
        "returned"
      ],
      "default": "active"
    },
    "notes": {
      "type": "string"
    },
    "overtime_scenario": {
      "type": "string",
      "enum": [
        "standard",
        "tiered",
        "flat_daily",
        "weekend_multiplier"
      ],
      "default": "standard"
    },
    "overtime_rate": {
      "type": "number",
      "default": 0
    },
    "overtime_tier1_multiplier": {
      "type": "number",
      "default": 1.25
    },
    "overtime_tier2_multiplier": {
      "type": "number",
      "default": 1.5
    },
    "overtime_tier2_threshold": {
      "type": "number",
      "default": 2
    },
    "overtime_flat_daily": {
      "type": "number",
      "default": 0
    },
    "overtime_weekend_multiplier": {
      "type": "number",
      "default": 1.5
    }
  },
  "required": [
    "name",
    "phone"
  ],
  "rls": {
    "read": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "create": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}